{{ define "content" }}
  <div class="lg:container mx-auto p-4">
    <h1 class="text-2xl font-semibold mb-4">Series</h1>
    {{ range .Series }}
      {{ template "series_card" . }}
    {{ end }}
  </div>
{{ end }}

